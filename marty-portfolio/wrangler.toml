# Used by `npx wrangler pages deploy dist`.
# Git-connected Pages projects can ignore this file and set
# root directory + build command in the Cloudflare dashboard.
name = "marty-portfolio"
pages_build_output_dir = "dist"
compatibility_date = "2026-09-09"
